import React from 'react';
import TopBar from './TopBar';
import SideBar from './SideBar';

const NavBar = () => {
    return (
        <>
            <TopBar />
            <SideBar />
        </>
    );
};

export default NavBar;
